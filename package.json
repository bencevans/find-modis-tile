{
  "name": "find-modis-tile",
  "version": "0.0.0",
  "dependencies": {
    "proj4": "^2.3.15"
  },
  "scripts": {
    "test": "ava",
    "test-ci": "nyc npm test && nyc report --reporter=text-lcov | coveralls"
  },
  "devDependencies": {
    "ava": "^0.17.0",
    "coveralls": "^2.11.15",
    "nyc": "^10.0.0"
  }
}
